plugins {
}

version = '0.1.1'
group = 'playerpets'

repositories {
}

dependencies {
}

subprojects {
}

allprojects {
    version = rootProject.version
    group = rootProject.group
}

task publish {
    group = 'Publishing'
    dependsOn ':playerpets-1.19.4:publish'
}